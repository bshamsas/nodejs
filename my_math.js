function addN(x, y){
    return (x + y);
}
function subtractN(x, y){
    return (x - y);
}

function multiplyN(x, y){
return ( x * y);
}
 
function divideN( x, y){
    return ( x / y);
}

 const pi = 3.14;
 module.exports={
    pi:pi,
    add:addN,
    subtract:subtractN,
    multiply: multiplyN,
    divide: divideN,

 }



